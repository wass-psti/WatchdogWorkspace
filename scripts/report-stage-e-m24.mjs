import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..');const read=(f)=>fs.readFileSync(path.join(root,f),'utf8');
const state=read('config/stage-e-m24-fueltrack-stabilization-target.ts').match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';
const m23=read('config/stage-e-m23-timetracker-stabilization-target.ts').match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';
const arch=read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1]??'unknown';
console.log('Stage E Milestone 24 FuelTrack+ stabilization status');console.log('--------------------------------------------------');
console.log(`M23 prerequisite state: ${m23}`);console.log(`M24 activation state: ${state}`);
console.log('Authoritative app runtime: app.v3.17.0-wm6.js');
console.log('Authoritative refresh before hydration: YES');
console.log('Preferences/activity workspace confirmed serial writes: YES');
console.log('Request/approval/refueling/delete duplicate-mutation gates: YES');
console.log('Failed critical writes reload authoritative request/activity state: YES');
console.log('Approval dialog closes only after confirmed decision: YES');
console.log('Cloud store synchronization: native storage + module fallback bridge');
console.log('Analytics: Apache ECharts 6.1.0 route-lazy enhancement + accessible fallback');
console.log('LightFuels stock deduction semantics: unchanged / no invented quantity deduction');
console.log('FuelTrack+ iframe compatibility island retained: YES');
console.log('New external dependency: echarts@6.1.0 (exact)');
console.log('Supabase migration required: NO');console.log(`Architecture version: ${arch}`);
console.log(state==='active-certified'?'\nM24 FuelTrack+ stabilization is release-certified.':'\nM24 implementation is complete and awaiting release certification.');
