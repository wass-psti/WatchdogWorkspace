import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..');
const read=(file)=>fs.readFileSync(path.join(root,file),'utf8');
const state=(file)=>read(file).match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';
const arch=read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1]??'unknown';
console.log('Stage F Milestone 29 Database/RLS test suite status');
console.log(`M28 prerequisite state: ${state('config/stage-f-m28-edge-functions-target.ts')}`);
console.log(`M29 activation state: ${state('config/stage-f-m29-database-rls-test-suite-target.ts')}`);
console.log(`Architecture version: ${arch}`);
console.log('Database test authority: pgTAP + Supabase CLI 2.117.0');
console.log('Public RLS tables covered structurally: 16');
console.log('Behavioral suites: account/RBAC, module-state authorization, Board authorization/realtime topic access');
console.log('Direct profile UPDATE bypass: RETIRED');
console.log('Direct module-role mutation bypass: RETIRED');
console.log('Safeguarded user-management RPC: RETAINED');
console.log('Production/linked database reset in certification: FORBIDDEN');
console.log('Runtime npm dependency added: NO');
console.log('Database migration required: YES — authorization hardening only');
