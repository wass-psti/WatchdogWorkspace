import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const target = read('config/stage-c-m14-command-shared-ui-target.ts');
const m13 = read('config/stage-c-m13-account-settings-user-management-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const state = target.match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const architecture = read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1] ?? 'unknown';
console.log('Stage C Milestone 14 Command Palette and Shared Application UI status');
console.log('-----------------------------------------------------------------');
console.log(`M13 prerequisite state: ${m13}`);
console.log(`M14 activation state: ${state}`);
console.log('React shared UI: src/app/shared-ui/SharedApplicationUI.tsx');
console.log('Shared UI runtime: src/app/shared-ui/shared-application-ui-runtime.ts');
console.log('Command registry authority: assets/js/features/commands/command-registry.ts');
console.log('Global overlay authority: assets/js/platform/ui/global-overlay-runtime.ts');
console.log('Shared surfaces: command palette + global toasts + service-worker update banner');
console.log(`Architecture version: ${architecture}`);
if (state === 'active-certified') console.log('\nM14 Command Palette and Shared Application UI architecture is release-certified.');
else if (state === 'active-pending-release-certification') console.log('\nM14 React shared-UI authority is active; full release certification remains pending.');
else if (state === 'implementation-complete-pending-certification') console.log('\nM14 implementation is complete and ready for release certification.');
else console.log('\nM14 is blocked until M13 is release-certified.');
