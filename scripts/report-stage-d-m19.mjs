import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const state = read('config/stage-d-m19-drag-drop-evaluation-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const prerequisite = read('config/stage-d-m18-virtualization-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const architecture = read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1] ?? 'unknown';
console.log('Stage D Milestone 19 Drag-and-drop evaluation status');
console.log('-----------------------------------------------------');
console.log(`M18 prerequisite state: ${prerequisite}`);
console.log(`M19 activation state: ${state}`);
console.log('Strategic candidate: @dnd-kit/react 0.5.0');
console.log('DOM bridge candidate: @dnd-kit/dom 0.5.0');
console.log('Legacy reference: @dnd-kit/core 6.3.1 + @dnd-kit/sortable 10.0.0');
console.log('Evaluation authority: src/app/boards/evaluation/dnd-kit-evaluation.ts');
console.log('Requirements authority: src/app/boards/evaluation/board-drag-drop-requirements.ts');
console.log('Production dependency installed: NO');
console.log('Adoption decision: defer-production-adoption');
console.log('Certified item drag authority: assets/js/features/boards/controllers/drag-drop-controller.ts');
console.log('Certified structure drag authority: assets/js/features/boards/controllers/structure-drag-controller.ts');
console.log(`Architecture version: ${architecture} (unchanged by evaluation-only M19)`);
console.log(state === 'active-certified' ? '\nM19 Drag-and-drop evaluation is release-certified.' : '\nM19 evaluation implementation is complete and awaiting release certification.');
