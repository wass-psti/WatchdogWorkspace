import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const state = read('config/stage-d-m21-rich-item-workspace-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const prerequisite = read('config/stage-d-m20-board-collaborative-realtime-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const architecture = read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1] ?? 'unknown';
console.log('Stage D Milestone 21 Rich Item Workspace status');
console.log('------------------------------------------------');
console.log(`M20 prerequisite state: ${prerequisite}`);
console.log(`M21 activation state: ${state}`);
console.log('Tabs: Overview / Updates / Files / Activity');
console.log('Property authority: existing Board command service + typed column registry');
console.log('Update draft resilience: YES');
console.log('Attachment drag-and-drop: YES');
console.log('Save on blur: NO');
console.log('New external dependency: NO');
console.log('Supabase migration required: NO');
console.log(`Architecture version: ${architecture}`);
console.log(state === 'active-certified' ? '\nM21 Rich Item Workspace is release-certified.' : '\nM21 Rich Item Workspace implementation is complete and awaiting release certification.');
