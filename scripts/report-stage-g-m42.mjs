import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..');const read=(f)=>fs.readFileSync(path.join(root,f),'utf8');const state=read('config/stage-g-m42-users-rbac-functional-recovery-target.ts').match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';const arch=read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1]??'unknown';console.log(`Stage G M42 Users / RBAC Functional Recovery status: ${state}`);console.log(`Architecture: ${arch}`);console.log('RBAC authority: serialized security-definer list_user_directory/admin_set_user_access with server policy flags and transactional module-role synchronization.');console.log('Safeguards: bootstrap admin, last active admin, self-disable, self-role reconciliation, and Admin-only mutation.');console.log('Production readiness: NOT YET — M43+ functional recovery and M54 production-readiness certification remain required.');
