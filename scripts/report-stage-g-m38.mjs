import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..');const read=(f)=>fs.readFileSync(path.join(root,f),'utf8');const state=read('config/stage-g-m38-runtime-configuration-backend-preflight-target.ts').match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';const arch=read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1]??'unknown';console.log(`Stage G M38 Runtime Configuration & Backend Capability Preflight status: ${state}`);console.log(`Architecture: ${arch}`);console.log('Runtime environments: local, development, CI, production');console.log('Capability classes: executable RPCs, required tables, private Storage, private Board Realtime authorization/policies/triggers');console.log('Dependent routes are module-specifically gated with explicit diagnostics until capability requirements pass.');console.log('Production readiness: NOT YET — M39+ functional recovery remains required.');
