import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const state = read('config/stage-d-m17-tanstack-table-evaluation-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const prerequisite = read('config/stage-d-m16-board-component-decomposition-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const architecture = read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1] ?? 'unknown';
console.log('Stage D Milestone 17 TanStack Table evaluation status');
console.log('-----------------------------------------------------');
console.log(`M16 prerequisite state: ${prerequisite}`);
console.log(`M17 activation state: ${state}`);
console.log('Candidate: @tanstack/react-table 9.2.4');
console.log('Evaluation authority: src/app/boards/evaluation/tanstack-table-evaluation.ts');
console.log('Requirements authority: src/app/boards/evaluation/board-table-requirements.ts');
console.log('Production dependency installed: NO');
console.log('Adoption decision: defer-production-adoption');
console.log('Compatibility Board engine: assets/js/boards-ui.ts');
console.log('Current Table renderer: assets/js/features/boards/views/table-view.ts');
console.log(`Architecture version: ${architecture} (unchanged by evaluation-only M17)`);
console.log(state === 'active-certified' ? '\nM17 TanStack Table evaluation is release-certified.' : '\nM17 evaluation implementation is complete and awaiting release certification.');
