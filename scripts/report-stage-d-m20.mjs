import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const state = read('config/stage-d-m20-board-collaborative-realtime-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const prerequisite = read('config/stage-d-m19-drag-drop-evaluation-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const architecture = read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1] ?? 'unknown';
console.log('Stage D Milestone 20 Board collaborative Realtime status');
console.log('--------------------------------------------------------');
console.log(`M19 prerequisite state: ${prerequisite}`);
console.log(`M20 activation state: ${state}`);
console.log('Transport authority: assets/js/platform/data/supabase-realtime-client.ts');
console.log('Board Realtime service: assets/js/features/boards/services/board-realtime-service.ts');
console.log('Board Realtime controller: assets/js/features/boards/controllers/board-realtime-controller.ts');
console.log('Realtime model: private database Broadcast + Presence + authorized RPC refetch');
console.log('Topic contract: board:<uuid> / event board-change');
console.log('Direct Postgres Changes: NO');
console.log('External Realtime dependency installed: NO');
console.log('Supabase migration required: YES');
console.log(`Architecture version: ${architecture}`);
console.log(state === 'active-certified' ? '\nM20 Board collaborative Realtime is release-certified.' : '\nM20 Board collaborative Realtime implementation is complete and awaiting release certification.');
