import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const target = read('config/stage-c-m12-authentication-ui-target.ts');
const m11 = read('config/stage-c-m11-global-overlays-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const state = target.match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const architecture = read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1] ?? 'unknown';
console.log('Stage C Milestone 12 Authentication UI status');
console.log('---------------------------------------------');
console.log(`M11 prerequisite state: ${m11}`);
console.log(`M12 activation state: ${state}`);
console.log('React authentication UI: src/app/auth/AuthenticationUI.tsx');
console.log('Authentication UI runtime: src/app/auth/authentication-ui-runtime.ts');
console.log('Supabase Auth/session authority: assets/js/core/auth.ts');
console.log(`Architecture version: ${architecture}`);
if (state === 'active-certified') console.log('\nM12 React authentication UI architecture is release-certified.');
else if (state === 'active-pending-release-certification') console.log('\nM12 React authentication UI authority is active; full release certification remains pending.');
else if (state === 'implementation-complete-pending-certification') console.log('\nM12 implementation is complete and ready for release certification.');
else console.log('\nM12 is blocked until M11 is release-certified.');
