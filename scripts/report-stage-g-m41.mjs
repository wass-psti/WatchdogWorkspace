import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..');const read=(f)=>fs.readFileSync(path.join(root,f),'utf8');const state=read('config/stage-g-m41-account-functional-recovery-target.ts').match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';const arch=read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1]??'unknown';console.log(`Stage G M41 Account Functional Recovery status: ${state}`);console.log(`Architecture: ${arch}`);console.log('Account authority: authenticated profile RPC, M39 access-context revalidation, Supabase Auth password update, and scoped session revocation.');console.log('Recovery behavior: password mutation and revocation outcomes are separated; failed global revocation preserves a retryable authenticated browser session.');console.log('Production readiness: NOT YET — M42+ functional recovery and M54 production-readiness certification remain required.');
