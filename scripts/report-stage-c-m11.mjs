import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const target = read('config/stage-c-m11-global-overlays-target.ts');
const m10 = read('config/stage-c-m10-react-shell-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const state = target.match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const architecture = read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1] ?? 'unknown';
console.log('Stage C Milestone 11 Global Overlays status');
console.log('-------------------------------------------');
console.log(`M10 prerequisite state: ${m10}`);
console.log(`M11 activation state: ${state}`);
console.log('React overlay host: src/app/overlays/GlobalOverlayHost.tsx');
console.log('Global runtime authority: assets/js/platform/ui/global-overlay-runtime.ts');
console.log('Overlay manager adapter: assets/js/platform/ui/overlay-manager.ts');
console.log(`Architecture version: ${architecture}`);
if (state === 'active-certified') console.log('\nM11 global overlay architecture is release-certified.');
else if (state === 'active-pending-release-certification') console.log('\nM11 global overlay authority is active; full release certification remains pending.');
else if (state === 'implementation-complete-pending-certification') console.log('\nM11 implementation is complete and ready for release certification.');
else console.log('\nM11 is blocked until M10 is release-certified.');
