import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..');
const read=(f)=>fs.readFileSync(path.join(root,f),'utf8');
const state=read('config/stage-e-m26-iframe-retirement-target.ts').match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';
const m25=read('config/stage-e-m25-tradelink-stabilization-target.ts').match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';
const arch=read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1]??'unknown';
console.log('Stage E Milestone 26 iframe retirement status');
console.log('------------------------------------------------');
console.log(`M25 prerequisite state: ${m25}`);
console.log(`M26 activation state: ${state}`);
console.log('Module presentation host: hybrid native-host / same-origin-iframe');
console.log('Iframe isolation is a per-module compatibility decision: YES');
console.log('Justified iframe retirements in M26: 0');
console.log('TimeTracker iframe retained: YES — native mount/style/DOM/identity/data/lifecycle/regression gates incomplete');
console.log('FuelTrack+ iframe retained: YES — native mount/style/DOM/identity/data/lifecycle/regression gates incomplete');
console.log('TradeLink iframe retained: YES — native mount/style/DOM/identity/data/lifecycle/regression gates incomplete');
console.log('Unsafe full-document DOM injection into host shell: PROHIBITED');
console.log('Native module registry available for future validated adapters: YES');
console.log('Direct host identity path for future native adapters: YES');
console.log('Native normalized-data port is module-scoped: YES');
console.log('Native mount failure publishes error/disposal lifecycle parity: YES');
console.log('New external dependency: NO');
console.log('Supabase migration required: NO');
console.log(`Architecture version: ${arch}`);
console.log(state==='active-certified'?'\nM26 iframe-retirement governance is release-certified.':'\nM26 implementation is complete and awaiting release certification.');
