import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const state = read('config/stage-d-m16-board-component-decomposition-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const prerequisite = read('config/stage-d-m15-react-board-presentation-facade-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const architecture = read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1] ?? 'unknown';
console.log('Stage D Milestone 16 Board component decomposition status');
console.log('--------------------------------------------------------');
console.log(`M15 prerequisite state: ${prerequisite}`);
console.log(`M16 activation state: ${state}`);
console.log('Facade composition authority: src/app/boards/BoardPresentationFacade.tsx');
console.log('Route component boundary: src/app/boards/components/BoardPresentationRouteBoundary.tsx');
console.log('Compatibility host component: src/app/boards/components/BoardPresentationSurface.tsx');
console.log('Derived route model: src/app/boards/components/board-presentation-model.ts');
console.log('Compatibility Board engine: assets/js/boards-ui.ts');
console.log('Board service/command/persistence authorities: existing typed Board layers');
console.log(`Architecture version: ${architecture}`);
console.log(state === 'active-certified' ? '\nM16 Board component decomposition architecture is release-certified.' : '\nM16 implementation is complete and awaiting release certification.');
