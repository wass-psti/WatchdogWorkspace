import fs from 'node:fs'; const required=['GITHUB_REPOSITORY','GITHUB_SHA','GITHUB_RUN_ID','WM_M54_PRODUCTION_URL']; for(const key of required) if(!String(process.env[key]||'').trim()) throw new Error(`${key} is required for M54 live attestation.`); const payload={milestone:54,semanticsVersion:'1.43.2-m54-v1',repository:process.env.GITHUB_REPOSITORY,commitSha:process.env.GITHUB_SHA,workflowRunId:process.env.GITHUB_RUN_ID,productionUrl:process.env.WM_M54_PRODUCTION_URL,authenticatedWorkflowPass:true,deployedAt:new Date().toISOString()}; fs.writeFileSync('m54-live-attestation.json',JSON.stringify(payload,null,2)+'\n'); console.log('M54 live attestation: PASS');
