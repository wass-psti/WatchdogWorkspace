import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const target = read('config/stage-c-m10-react-shell-target.ts');
const m9 = read('config/stage-b-m9-client-state-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const state = target.match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const architecture = read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1] ?? 'unknown';
console.log('Stage C Milestone 10 React Shell status');
console.log('---------------------------------------');
console.log(`M9 prerequisite state: ${m9}`);
console.log(`M10 activation state: ${state}`);
console.log('React shell authority: src/app/shell/WorkManagementShell.tsx');
console.log('Runtime bridge: src/app/shell/shell-runtime-bridge.ts');
console.log('Legacy route-content boundary: src/app/composition/LegacyApplicationBoundary.tsx');
console.log(`Architecture version: ${architecture}`);
if (state === 'active-certified') console.log('\nM10 React host shell architecture is release-certified.');
else if (state === 'active-pending-release-certification') console.log('\nM10 React shell authority is active; full release certification remains pending.');
else if (state === 'implementation-complete-pending-certification') console.log('\nM10 implementation is complete and ready for release certification.');
else console.log('\nM10 is blocked until M9 is release-certified.');
