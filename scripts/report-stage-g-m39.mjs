import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..');const read=(f)=>fs.readFileSync(path.join(root,f),'utf8');const state=read('config/stage-g-m39-auth-session-access-context-target.ts').match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';const arch=read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1]??'unknown';console.log(`Stage G M39 Authentication, Session & Access Context Stabilization status: ${state}`);console.log(`Architecture: ${arch}`);console.log('Session authority: persisted Supabase refresh session with explicit restoring/refresh/recovery lifecycle.');console.log('Access authority: public.wm_auth_access_context() atomic current-user profile/role/status/module-assignment snapshot.');console.log('Route authority: centralized Users and embedded-module RBAC policy with live authorization reconciliation.');console.log('Production readiness: NOT YET — M40+ presentation/lifecycle and module functional recovery remains required.');
