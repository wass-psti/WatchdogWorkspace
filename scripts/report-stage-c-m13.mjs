import fs from 'node:fs';
import path from 'node:path';
const root = path.resolve(import.meta.dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const target = read('config/stage-c-m13-account-settings-user-management-target.ts');
const m12 = read('config/stage-c-m12-authentication-ui-target.ts').match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const state = target.match(/activationState:\s*'([^']+)'/)?.[1] ?? 'unknown';
const architecture = read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1] ?? 'unknown';
console.log('Stage C Milestone 13 Account / Settings / User Management status');
console.log('--------------------------------------------------------------');
console.log(`M12 prerequisite state: ${m12}`);
console.log(`M13 activation state: ${state}`);
console.log('React management UI: src/app/management/AuthenticatedManagementUI.tsx');
console.log('Management UI runtime: src/app/management/authenticated-management-ui-runtime.ts');
console.log('Supabase Auth/RPC authority: assets/js/core/auth.ts');
console.log('Settings/backup authorities: assets/js/core/platform.ts + assets/js/core/backup.ts');
console.log('User-directory server state: TanStack Query 5.102.8');
console.log(`Architecture version: ${architecture}`);
if (state === 'active-certified') console.log('\nM13 Account / Settings / User Management architecture is release-certified.');
else if (state === 'active-pending-release-certification') console.log('\nM13 React management authority is active; full release certification remains pending.');
else if (state === 'implementation-complete-pending-certification') console.log('\nM13 implementation is complete and ready for release certification.');
else console.log('\nM13 is blocked until M12 is release-certified.');
