import fs from 'node:fs';
import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..');
const read=(file)=>fs.readFileSync(path.join(root,file),'utf8');
const state=(file)=>read(file).match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';
const arch=read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1]??'unknown';
console.log('Stage F Milestone 28 Edge Functions status');
console.log(`M27 prerequisite state: ${state('config/stage-f-m27-realtime-platform-target.ts')}`);
console.log(`M28 activation state: ${state('config/stage-f-m28-edge-functions-target.ts')}`);
console.log(`Architecture version: ${arch}`);
console.log('Edge Function authority: authenticated-governed-edge-functions-v1');
console.log('Production function: admin-sync-auth-access');
console.log('Gateway JWT verification: YES');
console.log('Live admin profile authorization: YES');
console.log('Server-only secret boundary: YES');
console.log('Exact browser origin allowlist: REQUIRED');
console.log('Automatic user-management cutover: NO — deployment must be certified first');
console.log('External npm dependency: NO');
console.log('Database migration/schema change: NO');
