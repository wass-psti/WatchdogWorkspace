import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..');const read=(f)=>fs.readFileSync(path.join(root,f),'utf8');const state=read('config/stage-g-m40-route-lifecycle-recovery-target.ts').match(/activationState:\s*'([^']+)'/)?.[1]??'unknown';const arch=read('config/application-manifest.ts').match(/architectureVersion:\s*(\d+)/)?.[1]??'unknown';console.log(`Stage G M40 React/Runtime Route Ownership & Lifecycle Recovery status: ${state}`);console.log(`Architecture: ${arch}`);console.log('Route authority: generation-based exclusive presentation ownership across shell, auth, management, Boards, and module-host surfaces.');console.log('Transition cleanup: route-scoped overlays, focus, pending management work, Board controllers, and embedded-module hosts are released deterministically.');console.log('Production readiness: NOT YET — M41+ functional module recovery and M54 production-readiness certification remain required.');
