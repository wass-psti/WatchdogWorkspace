# M32 — M31 historical verifier synchronization

M31 was certified at Architecture 39. M32 advances the application manifest to Architecture 40 while preserving all M31 performance authorities. The historical M31 verifier is therefore synchronized to accept Architecture 39 or later instead of requiring the manifest to remain exactly at 39. No M31 budget, benchmark, code-splitting, or activation requirement is weakened.
